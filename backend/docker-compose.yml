version: '3.8'

services:
  db:
    image: postgres:15
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: crm_backend_development
    ports:
      - "5432:5432"

  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: bash -c "rm -f tmp/pids/server.pid && bundle install && bundle exec rails db:prepare && bundle exec rails runner \"load Rails.root.join('db/queue_schema.rb').to_s unless ActiveRecord::Base.connection.table_exists?('solid_queue_jobs'); load Rails.root.join('db/cache_schema.rb').to_s unless ActiveRecord::Base.connection.table_exists?('solid_cache_entries'); load Rails.root.join('db/cable_schema.rb').to_s unless ActiveRecord::Base.connection.table_exists?('solid_cable_messages')\" && bundle exec rails server -b 0.0.0.0"
    volumes:
      - .:/rails
      - bundle_data:/usr/local/bundle
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgres://postgres:password@db:5432/crm_backend_development
      - RAILS_ENV=development
      # Usado pra montar a webhookUrl que o container do Baileys chama de volta — tem
      # que ser o nome do serviço no Compose (rede interna do Docker), não "localhost"
      # (que de dentro do container do baileys-api aponta pra ele mesmo, não pro Rails).
      - API_HOST=http://web:3000
      - JUERI_API_TOKEN=${JUERI_API_TOKEN:-}
      - JUERI_CLIENTE_SISTEMA=${JUERI_CLIENTE_SISTEMA:-}
      - JUERI_API_URL=${JUERI_API_URL:-}
    depends_on:
      - db
      - redis
      - baileys-api
    extra_hosts:
      - "host.docker.internal:host-gateway"

  worker:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: bash -c "bundle install && bundle exec rails solid_queue:start"
    volumes:
      - .:/rails
      - bundle_data:/usr/local/bundle
    environment:
      - DATABASE_URL=postgres://postgres:password@db:5432/crm_backend_development
      - RAILS_ENV=development
    depends_on:
      - db
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped

  baileys-api:
    image: ghcr.io/fazer-ai/baileys-api:latest
    ports:
      - "3025:3025"
    volumes:
      - baileys_storage:/app/storage
    environment:
      - NODE_ENV=production
      - PORT=3025
      - REDIS_URL=redis://redis:6379
      - REDIS_PASSWORD=
      - LOG_LEVEL=info
      - BAILEYS_LOG_LEVEL=error
      - BAILEYS_PROVIDER_DEFAULT_API_KEY=innovaweb2025
      - IGNORE_GROUP_MESSAGES=true
      - IGNORE_STATUS_MESSAGES=true
    command:
      - sh
      - "-c"
      - "bun manage-api-keys create user innovaweb2025 && bun start"
    depends_on:
      - redis
    restart: unless-stopped

volumes:
  postgres_data:
  bundle_data:
  baileys_storage:
