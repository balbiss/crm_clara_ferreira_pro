log/*
tmp/*

# Ignore master key for decrypting credentials and more.
/config/master.key

/log/*
/tmp/*

# Secrets locais (tokens de API de terceiros etc.) — nunca commitar.
.env
.env.*
!.env.example
