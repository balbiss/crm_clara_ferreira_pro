# ================================================================
# CRM CLARA FERREIRA ACESSÓRIOS — CONFIGURAÇÃO DE INSTALAÇÃO
# ================================================================
# 1. Copie este arquivo: cp .env.example .env
# 2. Preencha TODOS os campos marcados com ← OBRIGATÓRIO
# 3. Suba o sistema: docker compose -f docker-compose.prod.yml up -d
# ================================================================


# ── MARCA DA SUA EMPRESA ────────────────────────────────────────
APP_NAME="Clara Ferreira CRM"                # ← OBRIGATÓRIO
APP_LOGO_URL="https://seusite.com/logo.png" # URL pública da sua logo
APP_PRIMARY_COLOR="#FF007F"                 # Cor principal (hex) — Rosa Vibrante
APP_ACCENT_COLOR="#FF007F"                  # Cor de destaque (hex)
APP_SUPPORT_EMAIL="suporte@seusite.com"     # ← OBRIGATÓRIO
APP_FOOTER_TEXT="© 2026 Clara Ferreira Acessórios. Todos os direitos reservados."


# ── DOMÍNIOS ────────────────────────────────────────────────────
FRONTEND_URL=https://crm.seudominio.com     # ← OBRIGATÓRIO
BACKEND_URL=https://api.seudominio.com      # ← OBRIGATÓRIO


# ── BANCO DE DADOS ──────────────────────────────────────────────
POSTGRES_USER=clara
POSTGRES_PASSWORD=troque_essa_senha_agora   # ← OBRIGATÓRIO
POSTGRES_DB=clara_crm_production


# ── RAILS ───────────────────────────────────────────────────────
# Gere com: docker run --rm ruby:3.2-alpine ruby -e "require 'securerandom'; puts SecureRandom.hex(64)"
RAILS_MASTER_KEY=                           # ← OBRIGATÓRIO
SECRET_KEY_BASE=                            # ← OBRIGATÓRIO (mesmo comando acima)


# ── WHATSAPP (BAILEYS) ──────────────────────────────────────────
BAILEYS_API_KEY=troque_essa_chave_secreta   # ← OBRIGATÓRIO — qualquer string aleatória


# ── INSTAGRAM (DIRECT MESSAGES) ──────────────────────────────────
# App ID/Secret vêm do Meta for Developers (app usado pra OAuth + webhook)
INSTAGRAM_APP_ID=
INSTAGRAM_APP_SECRET=
# Qualquer string aleatória — precisa bater com o "Verify Token" configurado
# no painel de Webhooks da Meta
INSTAGRAM_WEBHOOK_VERIFY_TOKEN=


# ── META ADS (LEAD ADS / GERAÇÃO DE CADASTROS) ───────────────────
# App ID/Secret do app PRINCIPAL do Facebook (diferente do app filho usado
# só pro Instagram) — usa o produto "Login do Facebook para Empresas"
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=
# Reaproveita o mesmo INSTAGRAM_WEBHOOK_VERIFY_TOKEN acima pro handshake


# ── INTELIGÊNCIA ARTIFICIAL (OPCIONAL) ──────────────────────────
# Deixe em branco se não for usar a IA
OPENAI_API_KEY=
